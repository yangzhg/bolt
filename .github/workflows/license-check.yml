name: License Check

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  license-check:
    runs-on: ['self-hosted', 'small']
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check Licenses
        uses: apache/skywalking-eyes/header@v0.8.0
